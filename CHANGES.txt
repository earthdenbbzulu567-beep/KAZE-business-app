v3.0.7 — Fix homepage 500: sidebar groups no longer use the dict key name items (Jinja treated it as dict.items).

v3.0.6 — Payments: Stripe (Visa/Mastercard), PayPal, Flutterwave, and current-account transfers share one checkout path.

v3.0.5 — Faster requests: one settings load per request, cached unread badge, session cookie not rewritten every hit, extra indexes, gunicorn preload + 8 threads.

v3.0.4 — UI motion cleanup: one page/card entrance, no fake loading bar, no table-row stagger, one money count-up, ripple uses event delegation.

v3.0.3 — Sidebar grouping is data-driven: one NAV_GROUPS catalog, empty groups hidden, Ctrl+K uses the same list.

v3.0.2 — Sidebar tabs ordered: Core, Money, Trade, Growth, Workspace, Account.

v3.0.1 — Cleanup: fix nested document forms, remove dead price-parser and unused AI box, restore static logos, drop unused PythonAnywhere notes and sample JSON.

v3.0.0 — Flutterwave checkout on shop orders, Alpha Vantage price/FX refresh with 15-minute cache and 4s timeouts. Keys in Settings or env. Pages do not call APIs on load.

v2.9.9 — Supreme desks: banking books + transfers, investment holdings, online shop/orders/pay, monetization channels and market-value snapshots. CEO/Founder and Full mode. Books mode gets banking, Till gets shop, Office gets investments and market.

v2.9.8 — Hardcoded package prices (K): New user free, + 25, Entrepreneur 50, Entrepreneur + 75, Businessman 100, Businessman pro 150, CEO 300, Founder's premium 500.

v2.9.7 — Package prices are hardcoded in app.py (PLAN_TIERS). Settings cannot change them.

v2.9.5 — Packages: eight tiers (New user through Founder's premium).
Old Plans URL still works. Existing CEO accounts keep CEO.

v2.9.4 — Fix Render boot: create service_sessions before Stripe ALTER.

v2.9.3 — Bulk actions: integer IDs, FK-safe deletes, export without fake confirm,
stock/sales/customers/services/sessions/tasks bulk, nested-form fix.
Single stock/customer delete no longer 500s on linked rows.

v2.9.2 — Plans: New user, Businessman, CEO. Module + desk limits.
Stripe one-time upgrade, Settings override, plan badge in the sidebar.
Existing accounts stay CEO. New signups start as New user.

v2.9.1 — Stripe Checkout for service sessions, invoices, sales, and custom amounts.
Keys from Settings or env. Webhook /webhooks/stripe. Card data stays at Stripe.

v2.9.0 — Services module: catalog + session bookings for teaching, coaching,
repairs and similar work. Done sessions post income. Wired into search,
calendar, reminders, dashboard, backup, clear-data, modes, and multi-add.

KAZE v2.7.1 — review, fixes, two shop-floor features

Fixes
- Receipt and document PDFs now use Settings currency + decimals
- Login creates a settings row so currency / mode work on first visit
- Sales and customers lists use Settings → rows per page
- Sales charts stringify dates so Chart.js does not break
- Stock table shows an empty state when there are no products

New 1 — Multi-item till ticket
- Counter: tap several products onto one ticket
- Choose cash / mobile money / bank / card
- One discount for the whole ticket, stock drops per line

New 2 — Day close cash count
- Opening float + counted drawer
- Expected = float + cash sales + cash in − cash out
- Saves variance and last 14 closes
